<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.KeyVault" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="textInputStyle">@style/CustomTextInputEditText</item>
    </style>

    <style name="CustomTextInputEditText" parent="Widget.MaterialComponents.TextInputLayout.FilledBox">
        <item name="boxStrokeColor">@color/buttons</item>
        <item name="hintEnabled">true</item>
        <item name="hintTextColor">@color/card_selected</item>
        <item name="hintAnimationEnabled">true</item>
    </style>

</resources>